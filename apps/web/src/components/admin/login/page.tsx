import { brandConfig } from '@/config/brand';
import { themeConfig } from '@/config/theme';

export default function AdminLoginPage() {
    return (
        <div className="pt-8 pb-16 min-h-screen bg-white">
            <LoginButton />
        </div>
    );
}
